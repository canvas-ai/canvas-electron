#!/bin/bash

# Set the app root directory
ROOT=$(dirname $(dirname $(readlink -f $0)))

# Runtime defaults
# If --use-system-electron we should run electron /path instead

BIN="$ROOT/runtime/electron-linux-x64/electron"
APP="$ROOT/app/ui/electron $@"

$BIN $APP
