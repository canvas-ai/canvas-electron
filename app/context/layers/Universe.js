const Layer = require('../lib/Layer')
module.exports = new Layer({
    id: '/',    // 0? 
    name: 'universe',
    label: 'Universe',
    type: 'universe',
    description: 'And then there was light',
    color: '#fff'
})
