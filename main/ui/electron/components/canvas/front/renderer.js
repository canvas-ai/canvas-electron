// Electron
const {
  nativeImage,
  ipcRenderer
} = require('electron')



// Utils
//const spawn = remote.require('child_process').spawn

// UI Utils
const $ = require('jquery')
const path = require('path')
